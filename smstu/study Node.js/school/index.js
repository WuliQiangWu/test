var class = require('./class')
export.add = function(classes){
	classes.forEach(function(item,indx){
		var _class = item;
		var teacherName = item.teacherName
		var students = item.students
	})
	
	class.add(teacherName,students)
}
